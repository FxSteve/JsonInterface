using Newtonsoft.Json.Serialization;
using System;
using System.Collections.Generic;
using System.Text;

namespace JsonInterface.Extensions
{
    internal static class StringExtensions
    {
        //public static string ToCamelCase(this string value) =>
        //    (value?.Length).GetValueOrDefault() > 1 ?
        //    char.ToLowerInvariant(value[0]) + value.Substring(1) :
        //    value;

        //public static string NamingStrategyPropertyName(this  jsonInterfaceSettings, string propertyName)
        //{
        //    var contractResolver = jsonInterfaceSettings
        //        .DefaultIfNull()
        //        .JsonSerializerSettings
        //        .ContractResolver ?? new DefaultContractResolver();

        //    var namingStrategy = contractResolver.
        //}
    }
}